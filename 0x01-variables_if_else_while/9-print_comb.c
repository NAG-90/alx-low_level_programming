#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 48;

	putchar(i);
	while (i <= 57)
	{
		i++;
		putchar(44);
		putchar(32)
		putchar(i);
	}
	return (0);
}
